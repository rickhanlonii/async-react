import Layout from "@/components/Layout";
import FallbackList from "@/components/design/Fallback";

export default function Loading() {
  return (
    <Layout>
      <FallbackList />
    </Layout>
  );
}
